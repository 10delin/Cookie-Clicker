/**
 *
 * 1. Navegar hacia delante improvements
 * 2. Navegar hacia atrás improvements
 * 3. Clicar primer improvement
 * 4. Marcar como completado primer improvement
 * 5. Comprobar que el contenedor y el progressbar se acutalizan
 * 5. Volver hacia atrás
 * 6. Comprobar que el progress bar se ha actualizado
 * 7. Clicar de nuevo en el improvement
 * 8. Marcar como completado el segundo improvement
 * 9. Comprobar que la barra y el contenedo se ha actualizado
 * 10. Salir del improvement
 * 11. Comprobar que la barra se ha actualizado
 * 12. Clicar en el improvement
 * 13. Desmarcar el improvement y comprobar que se actualiza
 * 14. Cancelar el improvement y comprobar que se actualiza
 */

describe('Improvements Test', () => {
  // before(() => {
  //   cy.fixture('login.json').then((fixture) => {
  //     cy.login(fixture.email, fixture.password);
  //   });
  // });
  // it('Should navigate improvements section', () => {
  //   cy.getBySel('arrow-right').click({ force: true });
  //   cy.getBySel('arrow-right').click({ force: true });
  //   cy.getBySel('arrow-left').click({ force: true });
  //   cy.getBySel('arrow-left').click({ force: true });
  // });
});
